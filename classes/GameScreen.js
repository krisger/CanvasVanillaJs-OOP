class GameScreen{
    constructor(width, height, tag){
        this.width = width;
        this.height = height;
        this.tag = tag;     
    }
    
    setHeight(height){this.height = height;} 
    setWidth(width){this.width = width; }
    
}